# Vercel deployment

1. Upload all files in this folder to the root of the GitHub repository.
2. Make sure `.npmrc`, `package-lock.json`, `package.json`, and `vercel.json` are included.
3. In Vercel, redeploy without build cache.
4. Framework preset: Vite. Output directory: dist.
